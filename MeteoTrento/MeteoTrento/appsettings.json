{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Default log level for all logs
      "Microsoft.AspNetCore": "Warning" // Log level for Microsoft.AspNetCore namespace
    }
  },
  "AllowedHosts": "*" // Specifies that all hosts are allowed
}
