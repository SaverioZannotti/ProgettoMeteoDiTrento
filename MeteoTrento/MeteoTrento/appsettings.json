{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Livello di log predefinito per tutti i log
      "Microsoft.AspNetCore": "Warning" // Livello di log per il namespace Microsoft.AspNetCore
    }
  },
  "AllowedHosts": "*" // Specifica che tutti gli host sono consentiti
}
